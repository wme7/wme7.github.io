[project]
name = "pages"
version = "0.1.0"
description = "Manuel A. Diaz's Academic website"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pybtex>=0.26.1",
    "pyyaml>=6.0.3",
]
